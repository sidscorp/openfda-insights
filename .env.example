# Enhanced FDA Explorer Environment Configuration
# Copy this file to .env and fill in your actual API keys

# OpenFDA API (Optional - works without key but has rate limits)
FDA_API_KEY=

# AI Provider API Keys (Required for AI features)
# Choose one of the following:

# Option 1: OpenRouter (RECOMMENDED - access multiple models)
AI_PROVIDER=openrouter
AI_API_KEY=your_openrouter_api_key_here

# Option 2: OpenAI
# AI_PROVIDER=openai
# AI_API_KEY=your_openai_api_key_here

# Option 3: Anthropic Claude
# AI_PROVIDER=anthropic
# AI_API_KEY=your_anthropic_api_key_here

# Environment Settings
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# LangSmith Tracing (Optional - for debugging agent behavior)
# Sign up at https://smith.langchain.com to get an API key
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key_here
# LANGCHAIN_PROJECT=fda-agent

# Authentication (disabled by default)
AUTH_ENABLED=false
SECRET_KEY=your-super-secret-key-change-this

# Optional: Database URL (uses SQLite by default)
# DATABASE_URL=postgresql://user:pass@localhost/fda_explorer

# Optional: Redis for caching (uses in-memory cache by default)
# REDIS_URL=redis://localhost:6379

# GUDID Database Path (for device resolution)
GUDID_DB_PATH=/root/projects/analytics-projects/shared-data/gudid_full.db